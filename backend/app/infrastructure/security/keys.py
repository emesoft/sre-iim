"""Well-known keys used with the app_settings key-value store."""

CLAUDE_CLI_TOKEN_KEY = "claude_cli_token"

#: The active LLM provider and its per-provider settings, as one encrypted JSON blob.
LLM_CONFIG_KEY = "llm_config"
